name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build deployment directory
        run: |
          mkdir -p _site/dist
          cp -r docs/. _site/
          cp -r dist/apg-api _site/dist/apg-api
          cp -r dist/apg-lib _site/dist/apg-lib
          # Fix module import paths in the copied HTML so they point to ./dist/
          sed -i "s|from '\.\./dist/|from './dist/|g" _site/index.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
